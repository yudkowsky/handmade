#include <windows.h>

int CALLBACK
WinMain(HINSTANCE hInstance,
        HINSTANCE hPrevInstance,
        LPSTR IpCmdLin,
        int nCmdShow)
{
    MessageBox(0, "test test", "Test", MB_OK|MB_ICONINFORMATION);

	return(0);
}
